# scanner

# test bandit ssh

`ssh -p 5051 root@bandit "bandit -h"`
`ssh -p $OX_BANDIT_PORT root@$OX_BANDIT_HOST "bandit -h"`

# I am so pretty

npm install husky --save-dev
npm install --save-dev --save-exact prettier

# Start Redis before local run
docker run --rm --name redis-server -p 6379:6379 -d redis:6.2.6-alpine 
